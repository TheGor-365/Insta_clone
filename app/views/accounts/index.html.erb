<div class="container">
  <div class="row">
    <div class="col">
      <% @posts.each do |post| %>
        <%= image_tag post.image.url, class: 'img img-fluid' %>
      <% end %>
    </div>
  </div>
</div>
